# iPad版 抽選アプリ 仕様書（v0.9 ドラフト）

新興窯業株式会社 80周年記念パーティー用。PC版（`../抽選アプリ.html`）の演出・音の仕組みを引き継ぎつつ、
**iPad 1台で完結する、大幅にシンプルな版**として作り直す。

---

## 1. 全体像

| 項目 | 内容 |
|---|---|
| 使用機材 | iPad 1台のみ（プロジェクター投影なし・画面を直接見せる） |
| ネット環境 | **不要**（オフラインで完全動作） |
| 景品 | 5個 |
| 抽選回数 | 5回 |
| 1回の当選人数 | 1名（合計5名） |
| 順番 | 第5位 → 第4位 → 第3位 → 第2位 → 第1位 |
| ストップ方式 | **演出型のみ**（止めた後に演出して確定。番号は内部で公正に抽選） |
| 設定画面 | **なし**（最初の人数入力のみ） |
| 裏設定・除外番号機能 | **なし**（PC版の「9. 詳細設定」は移植しない） |

---

## 2. 画面構成（全3画面）

### 2-1. 初期設定画面（起動直後）

```
        新興窯業株式会社
        80周年記念抽選会

      参加人数を入力してください
         ┌─────────┐
         │   98    │
         └─────────┘
        1 〜 98 番の中から抽選します

         [  はじめる  ]
```

- 数字キーボードで参加人数を入力（`inputmode="numeric"`）
- 入力チェック：**5以上999以下**（5未満だと5回抽選できないため警告）
- 「はじめる」を押した時点で **音の有効化**（iOSは最初のタッチ前に音を出せないため）

### 2-2. 抽選画面（メイン）

```
┌──────────────────────────────────────┐
│ 🔊                              ⏻     │  ← 音量ON/OFF・リセット(長押し)
│                                      │
│              第 5 位                 │  ← 順位（大きく）
│                                      │
│           ┌──────────┐               │
│           │    47    │               │  ← ルーレット（1列）
│           └──────────┘               │
│                                      │
│           [  スタート  ]              │  ← 状態でボタンが切り替わる
│                                      │
│      新興窯業株式会社 80周年記念抽選会   │  ← 控えめに
└──────────────────────────────────────┘
```

**画面に出すもの：順位のみ**（賞名・景品名・当選履歴一覧・残り回数は出さない）

**ボタンの出方（状態で切り替え）**

| 状態 | 表示されるボタン |
|---|---|
| 待機中 | `スタート` |
| 回転中 | `ストップ` |
| 演出中（減速〜確定まで） | なし（押せない） |
| 結果表示中 | `再抽選` ／ `次へ`（最終回は `終了`） |

- ボタンは **指で押しやすい大きさ（縦60pt以上）**
- 回転中は「スタート」を出さない＝**二度押し事故が起きない**

### 2-3. 完了画面

- 5回終わったら「抽選終了 ありがとうございました」＋紙吹雪
- 隅のリセット（長押し）から初期設定画面に戻れる

---

## 3. 抽選ロジック

- 抽選対象プール：`1 〜 参加人数`
- **ストップを押した瞬間に**プールからランダムに1つ選び、それを当選番号として演出で着地させる
  （＝狙い撃ち不可。かつ「押した瞬間」に決めるので待ち時間による偏りもない）
- 当選した番号は**プールから除外**（二度当たらない）
- **辞退した番号もプールから永久除外**（以降の回で一切当たらない）
- 残りプールが0になった場合は警告を出して停止（人数入力ミスの保険）

---

## 4. 演出の割り当て（固定・当日は何も考えなくてよい）

PC版で完成した5種類の演出を、後半ほど盛り上がる順に固定配置する。

| 回 | 順位 | 演出 | 内部名 | 所要時間 |
|---|---|---|---|---|
| 1回目 | 第5位 | なめらか停止 | `smooth` | 約 5.8 秒 |
| 2回目 | 第4位 | 1ため | `standard` | 約 6.8 秒 |
| 3回目 | 第3位 | 行くと見せかけ | `stay` | 約 7.9 秒 |
| 4回目 | 第2位 | ロング | `tease` | 約 8.7 秒 |
| 5回目 | 第1位 | 2ため | `double` | 約 9.5 秒 |

### 動きの決まりごと（PC版から厳守で引き継ぐ）

- 回転速度は **1セル＝60ms**（約16.7セル/秒）
- **減速の開始速度が回転速度と一致**すること
  → 減速区間の長さ ＝ `90 × 流すセル数`（ms）
- 長く見せたい演出は「全速時間を足す」のではなく **流すセル数を増やす**
  （`smooth: +30セル` / `tease: +18セル`）
- 映像と音は**同じ軌道関数**を共有してズレを防ぐ
- セルの高さは **JSから明示指定**して、計算値と実寸を完全一致させる（右下がりズレの原因）

---

## 5. 再抽選（辞退）

用途：**役員などが当選し、辞退する場合**。

1. 結果表示中に `再抽選` をタップ
2. 確認ダイアログ：「**第3位の当選番号 47 を辞退として、もう一度抽選しますか？**」
3. OK → 47 を**永久除外**し、**同じ順位を同じ演出でもう一度**回す
4. 何度でも繰り返し可能

- 誤タップ防止のため必ず確認ダイアログを挟む
- `次へ` を押して次の順位に進んだ後は、前の回の再抽選はできない

---

## 6. 音（PC版と同じ仕様）

- **回転中**：一定テンポのメカっぽいカチカチ音（バンドパスノイズ＋低い胴鳴り。電子音・コミカル音はNG）
- **減速中**：映像と同じ軌道で、**数字が1つ進むたびに1回**鳴らす（自然にテンポが遅くなる）
- **停止音／当選音／ドラムロール／ファンファーレ（1位）**
- すべて **Web Audio API の合成音**（外部mp3ファイル不要＝オフラインで確実に鳴る）
- 画面隅に **音量ON/OFFボタン**

### iOS特有の対策

- 最初のタップ（「はじめる」）で `AudioContext` を起動
- **消音スイッチ／おやすみモードで無音になる問題**への対策：
  無音の `<audio playsinline>` を同時再生して再生セッションを確保する
- 本番前チェック手順：マニュアルに「音が出るか確認する」項目を明記

---

## 7. iPad / Safari 対応

| 項目 | 対策 |
|---|---|
| 画面が消える | **Screen Wake Lock API** で維持。非対応時は「設定→自動ロックをオフに」と画面に案内 |
| 画面の向き | **縦・横どちら向きでも破綻しない**レイアウト（数字が切れない） |
| 誤操作 | ピンチ拡大・ダブルタップ拡大・スクロールバウンド・長押し選択メニューを無効化 |
| ノッチ／ホームバー | `viewport-fit=cover` ＋ `env(safe-area-inset-*)` |
| バックグラウンド復帰 | アニメーションが止まっても**必ず正しい結果で終わる保険タイマー**を入れる |
| アドレスバー | ホーム画面に追加して全画面表示（`display: standalone`） |

---

## 8. 技術構成（ファイル一式）

```
80th/ipad/
├─ index.html      … アプリ本体（HTML+CSS+JS を1ファイルに）
├─ manifest.json   … ホーム画面追加の設定（アプリ名・アイコン・全画面）
├─ sw.js           … Service Worker（オフラインキャッシュ）
├─ icon-180.png    … ホーム画面アイコン
└─ 操作マニュアル.md … 当日用（別途作成）
```

- 外部ライブラリ・ビルド不要
- BroadcastChannel は**不要**（画面が1つだけになったため）
- 進行状況（何回目まで終わったか・当選番号・除外番号）を `localStorage` に自動保存し、
  誤ってアプリを閉じても続きから再開できるようにする

### 配布方法

**GitHub Pages** に置く（無料・https対応）。手順は完成時に別途案内する。
抽選アプリ自体に社外秘の情報は含まないため、URLが公開されても実害はない前提。

### 導入手順（当日までに）

1. 社内Wi-Fi環境で、配布URLをiPadのSafariで開く
2. 共有ボタン →「ホーム画面に追加」
3. **一度アプリを開いてリハーサル**（この時点でオフライン用のキャッシュが完成）
4. 当日は機内モードでも動作する

---

## 9. デザイン

参加者が直接見る画面なので、PC版の**観客画面（濃紺＋ゴールド）**を基調にする。

| 用途 | 色 |
|---|---|
| 背景 | `#1a1a2e` → `#16213e` グラデーション |
| ゴールド（主） | `#B8902F` |
| ゴールド（副） | `#C9A548` / `#D4A548` |
| 文字 | オフホワイト `#F8F5ED` |

- フォント：Georgia ＋ 游明朝（数字はセリフ体）
- ボタンは**ゴールドの細枠＋半透明**で、画面の雰囲気を壊さない上品な見た目に
- 「新興窯業株式会社　80周年記念抽選会」を画面下部に控えめに

---

## 10. PC版で判明した落とし穴（同じ失敗を繰り返さない）

1. **押した瞬間に止まって見える** → 減速区間 = `90 × セル数` で開始速度を回転速度に一致させる
2. **セル高さのズレ** → JSから高さを明示指定
3. **数字が見切れる** → 横方向に切り取らない。画面幅を基準に文字サイズを自動計算
4. **アニメーション停止** → 必ず結果で終わる保険タイマー
5. **キャッシュで古いコードが動く** → Service Worker に版番号を持たせ、更新時に確実に入れ替える

---

## 11. 第1位の特別演出

- 確定した瞬間に **ゴールドの紙吹雪**
- 当選音の代わりに **短いファンファーレ**
- カウントダウン（3・2・1）は**入れない**（シンプルさを優先）

他の回（第5位〜第2位）は、確定時に白フラッシュ＋当選音のみ。

---

## 12. 開発ステップ

動くものを小さく作って、そのつど確認しながら進める。

| ステップ | 作るもの | iPadで確認すること |
|---|---|---|
| 1 | 初期設定画面＋抽選画面の枠（回転なし・番号だけ切り替わる） | 文字が切れないか。縦横どちらでも崩れないか。ボタンが押しやすいか |
| 2 | ルーレット演出5種＋音 | 「押した瞬間に止まった」感じがしないか。音が鳴るか |
| 3 | 5回の進行・再抽選・完了画面・自動保存 | 通しで1回リハーサルできるか |
| 4 | ホーム画面追加＋オフライン化（manifest / Service Worker） | 機内モードで起動して最後まで動くか |
| 5 | 当日用の操作マニュアル | 手順どおりに他の人が操作できるか |

各ステップの途中は、PCのChrome（デベロッパーツールのiPad表示）でも確認できる。
